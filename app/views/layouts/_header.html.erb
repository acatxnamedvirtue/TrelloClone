<nav class="navbar">
  <div class="navbar-left-container group">
    <a class="navbar-brand-link" href="#">Boards</a>
  </div>

  <div class="navbar-brand-container group">
    <a class="navbar-brand-link" href="#">Trello Clone</a>
  </div>

  <div class="navbar-right-container">
    <ul class="navbar-right-list group">
    <% if signed_in? %>
      <li><%= button_to "Sign out", session_url, method: :delete%></li>
    <% else %>
      <li><%= link_to "Log in", new_session_url %></li>
      <li><%= link_to "Sign up", new_user_url %></li>
    <% end %>
    </ul>
  </div>
</nav>

<% if flash[:errors] %>
<div class="alert alert-warning alert-dismissible" role="alert">
   <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
<%= flash[:errors].join("<br>").html_safe %>
</div>
<% end %>
